{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "batchcraft-browser": {
      "type": "local",
      "command": ["npm", "--prefix", "frontend", "run", "--silent", "browser:mcp"],
      "enabled": true
    }
  }
}
